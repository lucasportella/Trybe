n = 5;
let inputLine = '';
let asterisk = '*'

for (counter = 0; counter < n; counter += 1) {
    inputLine = inputLine + asterisk;
}
for (counter = 0; counter < n; counter += 1) {
    console.log(inputLine)
}